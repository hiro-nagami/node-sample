version: '3'
services:
  node:
    build: .
    tty: true
    ports:
      - "0.0.0.0:5001:3000"
    env_file:
      - .env
    volumes:
      - ".:/app"
    working_dir: /app
    command: yarn dev
    networks:
      - default
      - sample-shared
networks:
  sample-shared:
    external: true